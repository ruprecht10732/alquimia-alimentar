# Optional environment variables.
# The app defaults to port 8080, which matches the Dockerfile EXPOSE directive.
# Coolify reads EXPOSE 8080 automatically, so PORT usually does not need to be set.
# PORT=8080
